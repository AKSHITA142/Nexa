{
  "name": "personal-assistant-nexora",
  "version": "1.0.0",
  "private": true,
  "description": "NEXORA AI Personal Assistant & Automation Command Center",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
